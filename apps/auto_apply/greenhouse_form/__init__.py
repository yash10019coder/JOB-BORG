"""Greenhouse application-page browser-automation client (DB-free).

See ``client.GreenhouseFormClient`` for the public entry points
(``inspect()``/``submit()``) and ``exceptions.py`` for the typed failure
hierarchy this package raises.
"""
